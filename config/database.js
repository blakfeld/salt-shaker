database = {
    'server': 'localhost',
    'database': 'salt-shaker-database',
    'username': '',
    'password': ''
};

/**
 * Export Database Config
 * @type {{server: string, database: string, username: string, password: string}|*}
 */
module.exports = database;
